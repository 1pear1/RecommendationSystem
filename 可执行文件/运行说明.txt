利用pyinstaller对代码打包在dist文件夹中生成.exe可执行文件。
但是双击exe文件会发现，代码运行后，一闪而过（闪退）。

可cd进入到对应文件夹中
.\xxx.exe

其中 MLP的执行命令为
.\mlp.exe split1 #全局随机划分训练
.\mlp.exe split2 #按用户划分训练
.\mlp.exe search #搜索最佳参数
.\mlp.exe test split1 #用 split1 模型测试
.\mlp.exe test split2 #用 split2 模型测试
